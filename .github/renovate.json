{
  "$schema": "https://docs.renovate.bot/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],

  "timezone": "UTC",
  "schedule": ["before 3am"],
  
  "vulnerabilityAlerts": {
    "enabled": true,
    "osvVulnerabilityAlerts": true
  },

  "enabledManagers": ["custom.regex"],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "public-images/**/*.versions"
      ],
      "matchStrings": [
        "(?<depName>.*?):(?<currentValue>.*)"
      ],
      "datasourceTemplate": "docker"
    }
  ],

  "packageRules": [
    {
      "description": "Disable minor and major updates, but ALLOW patch updates",
      "matchManagers": ["custom.regex"],
      "matchUpdateTypes": [
        "major",
        "minor"
      ],
      "enabled": false,
      "groupName": "Base Image Version Updates",
      "groupSlug": "base-images-regex"
    }
  ]
}