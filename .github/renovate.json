{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "platform": "github",
  "repositories": [
    "ianhong/docker-renovate"
  ],
  "timezone": "UTC",
  "schedule": [
    "before 3am"
  ],
  "prHourlyLimit": 2,
  "prConcurrentLimit": 5,
  "enabledManagers": [
    "custom.regex"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "(\\./)?public-images/(?<depName>.*?)\\.versions$",
        "(\\./)?(?<depName>[^/]+?)\\.versions$"
      ],
      "matchStrings": [
        "^(?<currentValue>[a-zA-Z0-9._-]+)$"
      ],
      "datasourceTemplate": "docker",
      "depNameTemplate": "{{depName}}"
    }
  ],
  "packageRules": [
    {
      "matchManagers": [
        "custom.regex"
      ],
      "groupName": "Base Image Version Updates",
      "groupSlug": "base-images-regex",
      "minimumReleaseAge": "3 days"
    }
  ]
}